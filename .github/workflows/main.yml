name: My First CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Run a simple command
      run: echo "Hello, GitHub Actions!"

    - name: List files
      run: ls -la
